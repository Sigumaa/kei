関数 L0(r1, r2) を定義する。
  もし r2 が 0 なら、以下を行う。
    L2(r1, r2) を返す。
  以上。
  そうでなければ、以下を行う。
    r2 から 1 を減じた数を r2 とする。
    L1(r1, r2) を返す。
  以上。
以上。

関数 L1(r1, r2) を定義する。
  r1 に 1 を加えた数を r1 とする。
  L0(r1, r2) を返す。
以上。

関数 L2(r1, r2) を定義する。
  r1 を返す。
以上。

主文() を関数として定義する。
  a は 3 とする。
  b は 4 とする。
  ans は L0(a, b) とする。
  ans を出力する。
以上。
