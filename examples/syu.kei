関数 交互和(n) を関数として定義する。
合 は 0 とする。
現在 は 1 とする。
符号 は 1 とする。
n 回、以下を行う。
  現在 と 符号 の積を 一項 とする。
  合 に 一項 を加えた数を 合 とする。
  符号 と -1 の積を 符号 とする。
  現在 に 1 を加えた数を 現在 とする。
以上。
合 を返す。
以上。

関数 和(n) を関数として定義する。
合 は 0 とする。
現在 は 1 とする。
n 回、以下を行う。
  合 に 現在 を加えた数を 合 とする。
  現在 に 1 を加えた数を 現在 とする。
以上。
合 を返す。
以上。

関数 主文() を関数として定義する。
「主文の開始」を出力する。
交互和(5) を出力する。
和(10) を出力する。
「主文の了」を出力する。
以上。
